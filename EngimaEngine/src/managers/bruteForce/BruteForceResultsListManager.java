package managers.bruteForce;

import bruteForce.BruteForceResultDTO;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;

public class BruteForceResultsListManager {

/*
    private final Map List<BruteForceResultDTO> bruteForceResultDTOListManager;

    public BruteForceResultsListManager() {
        bruteForceResultDTOListManager = new ArrayList<>();
    }

    public synchronized void addruteForceResultDTO(BruteForceResultDTO bruteForceResultDTO) {
        bruteForceResultDTOListManager.add(bruteForceResultDTO));
    }

    public synchronized List<BruteForceResultDTO> getbruteForceResultDTOEntries(int fromIndex){
        if (fromIndex < 0 || fromIndex > bruteForceResultDTOListManager.size()) {
            fromIndex = 0;
        }
        return bruteForceResultDTOListManager.subList(fromIndex, bruteForceResultDTOListManager.size());
    }

    public int getVersion() {
        return bruteForceResultDTOListManager.size();
    }
*/


}
